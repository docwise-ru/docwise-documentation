import type { SearchSuggestPageItem } from '@diplodoc/components';
import type { Position, Registry, WorkerConfig } from '../types';
import type { SearchResult } from './search';
import type { Score } from './score';
type Trimmer = (text: string, score: Score) => [string, Position[]];
export declare function format({ base, mark }: Pick<WorkerConfig, 'base' | 'mark'>, results: SearchResult[], registry: Registry, trim: Trimmer): SearchSuggestPageItem[];
export declare function short(text: string, score: Score): [string, Position[]];
export declare function long(text: string, score: Score): [string, Position[]];
export {};
