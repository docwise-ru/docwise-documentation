import type { IndexSettings, SearchParamsObject } from 'algoliasearch';
import type { BuildRun, EntryInfo, SearchProvider } from '@diplodoc/cli';
import { AlgoliaProviderConfig } from '../types';
export declare class AlgoliaProvider implements SearchProvider {
    private index;
    private appId;
    private apiKey?;
    private searchApiKey?;
    private indexName;
    private indexSettings;
    private querySettings;
    private objects;
    private client?;
    private run;
    private logger;
    private apiLink;
    private workerPool?;
    constructor(run: BuildRun, config: AlgoliaProviderConfig);
    add(path: string, lang: string, info: EntryInfo): Promise<void>;
    addObjects(): Promise<void>;
    clearIndex(): Promise<void>;
    setSettings(settings: Partial<IndexSettings>): Promise<void>;
    release(): Promise<void>;
    config(lang: string): {
        provider: string;
        api: string;
        link: string;
        appId: string;
        indexName: string;
        searchApiKey: string | undefined;
        querySettings: Partial<SearchParamsObject>;
    };
    private createIndexName;
    private ensureClient;
    private processDocumentSync;
    private uploadRecordsToAlgolia;
}
