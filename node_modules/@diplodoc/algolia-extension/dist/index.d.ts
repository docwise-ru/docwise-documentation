import type { BaseConfig, IExtension } from '@diplodoc/cli/lib/program';
import { BaseArgs, BaseProgram } from '@diplodoc/cli/lib/program';
import { Command, ExtendedOption } from '@diplodoc/cli/lib/config';
import { AlgoliaConfig } from './types';
export declare class AlgoliaProgram extends BaseProgram<AlgoliaConfig> {
    readonly name = "index";
    readonly command: Command;
    readonly options: ExtendedOption[];
    protected readonly modules: never[];
    private run;
    action(args: BaseArgs): Promise<void>;
    private validateAndGetConfig;
    private createProvider;
}
interface SearchConfig {
    appId?: string;
    apiKey?: string;
    indexName?: string;
    searchApiKey?: string;
    indexPrefix?: string;
    provider?: string;
    index?: boolean;
    api?: string;
}
interface ExtensionConfig extends BaseConfig {
    search?: SearchConfig;
}
export declare class Extension implements IExtension {
    private static hooksRegistered;
    apply(program: BaseProgram<ExtensionConfig>): void;
    private addAlgoliaModule;
    private registerBuildHooks;
    private createAlgoliaProvider;
}
export default Extension;
