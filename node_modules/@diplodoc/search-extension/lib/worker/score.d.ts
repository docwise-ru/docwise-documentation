import type { Index } from 'lunr';
import type { Position } from '../types';
export type Score = {
    positions: Position[];
    score: number;
    position: Position;
};
export declare function sparsed(result: Index.Result): Record<string, Score>;
export declare function phrased(result: Index.Result, terms: string[]): Record<string, Score>;
