import { AlgoliaRecord, DocumentMeta } from '../types';
export declare class AlgoliaWorkerPool {
    private workers;
    private queue;
    private processing;
    private readonly maxWorkers;
    private readonly workerPath;
    private results;
    private resolvePromise;
    private logger;
    constructor(maxWorkers?: number);
    initialize(): void;
    addTask(path: string, lang: string, html: string, title: string, meta: DocumentMeta, skipHtmlExtension?: boolean): void;
    waitForCompletion(): Promise<Map<string, AlgoliaRecord[]>>;
    terminate(): Promise<void>;
    private createWorker;
    private handleWorkerMessage;
    private replaceWorker;
    private processNextItem;
    private startProcessing;
    private checkCompletion;
}
