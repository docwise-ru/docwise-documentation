import type { Index } from 'lunr';
import type { Score } from './score';
import type { WorkerConfig } from '../types';
export type SearchResult = Index.Result & {
    scores: Record<string, Score>;
};
export declare function search({ tolerance, confidence }: Pick<WorkerConfig, 'confidence' | 'tolerance'>, index: Index, query: string, count: number, sealed?: boolean): SearchResult[];
